name: sklad
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  path: ^1.8.0
  image_picker: ^0.8.4+4
  flutter_barcode_scanner: ^2.0.0
  barcode_scan2: ^4.2.0
  provider: ^6.0.0
  collection: ^1.16.0 # Для методов коллекций, если необходимо
  intl: ^0.18.0 # Добавьте эту строку

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true